export default class userModel {
    constructor(userName, email, password, bio, id){
        this.userName = userName;
        this.email = email;
        this.password = password;
        this.bio = bio
        this.id = id;
    }
}